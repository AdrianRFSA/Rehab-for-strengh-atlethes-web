---
export const prerender = true;
import { testimonials } from "@/lib/testimonials";
import BaseLayout from "@/layouts/BaseLayout.astro";
import { CustomCard } from "@/components/testimonials/react/CustomCard";
import Hero from "@/components/hero/Hero.astro";
import ContactCTA from "@/components/cta/ContactCTA.astro";
---

<BaseLayout
  title="Atletas | Resultados reales en rehabilitación deportiva"
  description="Conoce los resultados reales de atletas que han pasado por el proceso de rehabilitación con Rehab for Strength Athletes."
>
  <div class="stack gap-20 lg:gap-48 bg-color-dark bg-color-ligth">
    <main class="wrapper stack mt-20 justify-center items-center gap-8">
      <Hero
        title="Atletas | Confianza y Resultados"
        tagline="Estos atletas confiaron en Rehab for Strength Athletes y sus resultados hablan por sí solos"
        align="start"
      />
      <section class="w-full max-w-7xl mx-auto">
        <div class="grid grid-cols-1 md:grid-cols-2 gap-8 justify-items-center">
          {
            testimonials.map((testimonial) => (
              <div class="w-full max-w-md">
                <li
                  class="p-4 list-none"
                  transition:name={`card-${testimonial.name}`}
                >
                  <CustomCard
                    client:load
                    url={testimonial.url}
                    title={testimonial.name}
                    subtitle={testimonial.sport}
                    description={testimonial.testimonial}
                    image={testimonial.image}
                  />
                </li>
              </div>
            ))
          }
        </div>
      </section>
    </main>
    <ContactCTA />
  </div>
</BaseLayout>
